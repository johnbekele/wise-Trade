# MongoDB Configuration
# Replace with your actual MongoDB connection string
MONGO_URI=mongodb://localhost:27017
MONGO_DATABASE=wise_trade_db
MONGO_COLLECTION=users

# SMTP Configuration (for email verification)
# Replace with your actual SMTP credentials
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password

# JWT Configuration
# IMPORTANT: Generate strong, unique secret keys for production
# Use: openssl rand -hex 32 to generate secure keys
SECRET_KEY=REPLACE_WITH_ACTUAL_SECRET_KEY
REFRESH_SECRET_KEY=REPLACE_WITH_ACTUAL_REFRESH_SECRET_KEY
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
TOKEN_EXPIRE_DAYS=7
TOKEN_EXPIRE_MINUTES=30
